/* Printing a triangle of 50 lines up. */
for (let iterator = 1; iterator <= 50; iterator++) {
	console.log('-'.repeat(iterator))
}

/* Printing a triangle of 50 lines down. */
for (let iterator = 50; iterator >= 0; iterator--) {
	console.log('-'.repeat(iterator))
}

