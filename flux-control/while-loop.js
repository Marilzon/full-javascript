/* Creating an array and iterator variable. */
let target = 50
let iterator = 0

/* Iterating through the array and printing each item. */
while (iterator < target) {
	console.log('+'.repeat(iterator))

	iterator ++
}
